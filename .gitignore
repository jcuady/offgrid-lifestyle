node_modules/
build/
dist/
coverage/
.DS_Store
__MACOSX/
*.log
.env*
!.env.example

# Local credentials / export artifacts. Keep secrets out of git.
*credentials*
*service-role*
*service_role*
*vapid-private*

.vercel

# Local security review notes (not for git)
Security (1).md

# Local dev memory with secrets — never commit
.dev-memory*.md

# Deploy/MCP scratch artifacts (may contain function source)
call-*.json
mcp-*.json
mcp-invoke-*.json
deploy-result-*.json
deploy-out-*.json
deploy-submit.json
tmp-deploy-*.json
tmp-mcp/
.hook-secret.tmp

# Supabase CLI temp
supabase/.temp/

# Playwright e2e artifacts
test-results/
playwright-report/
.playwright/

# Large local design-source assets (not used by the build; exceed GitHub size limits)
og photos/
Offgrid - Branding Guide/
Materials-*/
dist/OG logo/
*.zip
