/**
 * @deprecated Import from `@/src/lib/orderLifecycle` — re-exported for existing callers.
 */
export {
  ADMIN_ORDER_TRANSITIONS,
  ORDER_TRANSITIONS,
  PAYMENT_TRANSITIONS,
  STATUS_FLOW,
  canOverridePaymentStatus,
  canTransitionStatus,
} from "@/src/lib/orderLifecycle";
